name: ghost_signal
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.0.0 # Changed from 3.9.2 (which doesn't exist yet) to a standard stable version

dependencies:
  flutter:
    sdk: flutter

  # The "Senses" & UI
  cupertino_icons: ^1.0.8
  geolocator: ^13.0.0 # Updated to standard stable version
  vibration: ^1.9.0 # Updated to compatible version
  torch_light: ^1.0.2
  url_launcher: ^6.3.0
  google_fonts: ^6.2.1
  percent_indicator: ^4.2.3
  tflite_audio: ^0.3.0
  nearby_connections: ^4.3.0
  sensors_plus: ^7.0.0
  permission_handler: ^12.0.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # FIXED ASSETS SECTION
  assets:
    - assets/soundclassifier.tflite
    - assets/labels.txt
